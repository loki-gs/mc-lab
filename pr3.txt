AREA FACT, CODE, READONLY
START
	MOV R0,#5
	MOV R1, #1
	CMP R0, #0
	BEQ STOP
	MOV R1, R0
NEXT SUBS R0, #1
	CMP R0, #0
BEQ STOP
	MUL R2,R1,R0
        MOV R1,R2
	B NEXT
STOP
	NOP
        END
